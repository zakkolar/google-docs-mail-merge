{
  "name": "mail-merge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "watch": {
    "dev": {
      "patterns": [
        "src"
      ],
      "extensions": "js,html,css,scss,ts"
    }
  },
  "scripts": {
    "watch": "npm-watch",
    "compile": "node compile.js",
    "build": "node compile",
    "deploy": "clasp push",
    "dev": "npm run build && npm run deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-watch": "^0.6.0"
  },
  "devDependencies": {
    "fs-extra": "^8.0.0",
    "glob": "^7.1.4",
    "path": "^0.12.7",
    "sass": "^1.20.1"
  }
}
